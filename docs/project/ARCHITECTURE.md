# Architecture

Template note: fill this file in the repository that adopts SpecOps Overlay.
Until then, the fields below are placeholders, not project facts.

Use this file to describe stable architecture decisions for the adopting
repository.

## Purpose

- System boundaries and ownership.
- Module and bounded-context responsibilities.
- Deployment/runtime shape when it affects design choices.
- Architectural decisions that should guide future changes.

## Fill After Adoption

- Primary runtime components:
- Important modules or bounded contexts:
- Data ownership boundaries:
- Synchronous communication paths:
- Asynchronous communication paths:
- Key architectural decisions:
- Known architecture constraints:

## OpenSpec Use

Reference this file from `openspec/config.yaml` as engineering context for
proposals, designs, and implementation tasks. Put observable system behavior in
`openspec/specs/`, not here.
