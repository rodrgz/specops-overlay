# Conventions

Template note: fill this file in the repository that adopts SpecOps Overlay.
Until then, the fields below are placeholders, not project facts.

Use this file for repository-specific coding, naming, API, data, and review
conventions in the adopting repository.

## Fill After Adoption

- Module and file naming:
- Function, method, or class naming:
- API naming:
- Data transfer and validation conventions:
- Error handling conventions:
- Transaction or persistence conventions:
- Logging conventions:
- Commit or branch conventions:

## OpenSpec Use

Use these conventions when writing OpenSpec designs and tasks. If a proposed
change needs to break a convention, document the reason in the design.

## Commit Guidance

After adoption, document the repository's exact commit policy here. Until a
project-specific policy replaces this placeholder, OpenSpec tasks should
recommend one focused Conventional Commit per non-trivial implementation task
when the repository workflow allows it.

Examples:

- `feat(api): add invite endpoint`
- `fix(auth): preserve tenant boundary`
- `docs(tasks): add proof mapping`
