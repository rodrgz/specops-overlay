## Why

<!-- Explain the motivation for this change. What problem does this solve? Why now? -->

## What Changes

<!-- Describe what will change. Be specific about new capabilities, modifications, removals, or breaking changes. -->

## Capabilities

### New Capabilities
<!-- Capabilities being introduced. Replace <name> with a kebab-case identifier. Each creates specs/<name>/spec.md. -->
- `<name>`: <brief description of what this capability covers>

### Modified Capabilities
<!-- Existing capabilities whose REQUIREMENTS are changing. Use existing spec names from openspec/specs/. Leave empty if none. -->
- `<existing-name>`: <what requirement is changing>

### Removed Capabilities
<!-- Capabilities or requirements being removed. Leave empty if none. -->
- `<existing-name>`: <what is being removed and why>

## Impact

- Affected areas:
  - `<paths, modules, docs, scripts, schemas, templates, or n/a>`
- Stack/flavor guidance: `<not relevant or selected flavor>`
- Observable behavior: `<what changes from a user/contributor/tool perspective>`
- Risk level: `<small|medium|large|complex>` because `<reason>`
- Spec quality gate: `<required|not required|skipped>` because `<reason>`
- Out of scope:
  - `<explicit future behavior, upstream change, or n/a>`
